kafka:
  servers: localhost:9092
  partition: 4
  payment-topic: payment
  payment-application-name: settlement-streams

logging:
  level:
    root: INFO
    org.apache.kafka.streams: INFO
    org.apache.kafka.clients: INFO
