kafka:
  servers: localhost:9092
  partition: 4
  payment-topic: test-payment
  payment-application-name: test-settlement-streams

logging:
  level:
    root: INFO
    org.apache.kafka.streams: INFO
    org.apache.kafka.clients: INFO
